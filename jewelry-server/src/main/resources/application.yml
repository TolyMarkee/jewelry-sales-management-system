server:
  port: 8088

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/jewelry_db?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: Wj686866
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.jewelry.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-value: 1
      logic-not-delete-value: 0

# JWT配置
jwt:
  secret: jewelry-secret-key-2024-jewelry-sales-management-system
  expire: 86400000  # 24小时

# AI大模型配置（可选，不配置则使用内置回复）
# 支持 OpenAI / DeepSeek / 通义千问 等兼容接口
ai:
  api:
    url:                          # 如: https://api.openai.com/v1/chat/completions
    key:                          # 你的 API Key
    model: gpt-3.5-turbo          # 模型名称
